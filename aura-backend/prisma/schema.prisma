generator client {
  provider      = "prisma-client-js"
  // Docker image uses node:22-bookworm-slim (OpenSSL 3); without this, runtime can miss the query engine.
  binaryTargets = ["native", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ImageLog {
  id                 String     @id @default(uuid())
  imageUrl           String
  objectKey          String
  status             ScanStatus @default(PENDING)
  nsfwScore          Float?
  violenceScore      Float?
  topLabels          Json?
  reasonShort        String?
  modelVersion       String?
  labelSetVersion    String?
  thresholdsVersion  String?
  scoresFull         Json?
  workerVersion      String?
  processedTimeMs    Int?
  createdAt          DateTime   @default(now())
}

enum ScanStatus {
  PENDING
  SAFE
  UNSAFE
  ERROR
}
